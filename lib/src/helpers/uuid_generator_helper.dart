import 'package:flutter/material.dart';

import 'package:uuid/uuid.dart';

class UuidGeneratorHelper {
  // print('CLAVEJAJAJJAA: ${Uuid().v5(Uuid.NAMESPACE_URL, androidInfo.androidId)}'); //ID unico compuesto de androidInfo.androidId(que es id unico de dispositivo)
}